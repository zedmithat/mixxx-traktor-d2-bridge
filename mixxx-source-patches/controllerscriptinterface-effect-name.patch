diff --git a/src/effects/effectslot.h b/src/effects/effectslot.h
--- a/src/effects/effectslot.h
+++ b/src/effects/effectslot.h
@@ -61,6 +61,10 @@ class EffectSlot : public QObject {
             EffectChain* pChainSlot,
             EngineEffectChain* pEngineEffectChain);
     virtual ~EffectSlot();
+
+    /// Thread-safe display-name snapshot for legacy controller scripts.
+    /// Returns an empty string when the slot is empty or unknown.
+    static QString displayNameForGroup(const QString& group);
 
     inline int getEffectSlotNumber() const {
         return m_iEffectNumber;
diff --git a/src/effects/effectslot.cpp b/src/effects/effectslot.cpp
--- a/src/effects/effectslot.cpp
+++ b/src/effects/effectslot.cpp
@@ -1,6 +1,8 @@
 #include "effects/effectslot.h"
 
 #include <QDebug>
+#include <QHash>
+#include <QReadWriteLock>
 
 #include "control/controlencoder.h"
 #include "control/controlpushbutton.h"
@@ -22,5 +24,24 @@
 
 // The maximum number of effect parameters we're going to support.
 constexpr unsigned int kDefaultMaxParameters = 16;
+
+namespace {
+QReadWriteLock s_effectNameLock;
+QHash<QString, QString> s_effectNames;
+
+void setCachedEffectName(const QString& group, const QString& name) {
+    QWriteLocker locker(&s_effectNameLock);
+    if (name.isEmpty()) {
+        s_effectNames.remove(group);
+    } else {
+        s_effectNames.insert(group, name);
+    }
+}
+} // namespace
+
+QString EffectSlot::displayNameForGroup(const QString& group) {
+    QReadLocker locker(&s_effectNameLock);
+    return s_effectNames.value(group);
+}
 
 EffectSlot::EffectSlot(const QString& group,
@@ -137,5 +158,6 @@ EffectSlot::~EffectSlot() {
     //qDebug() << debugString() << "destroyed";
     unloadEffect();
+    setCachedEffectName(m_group, QString());
 }
 
 void EffectSlot::addToEngine() {
@@ -360,6 +382,7 @@ void EffectSlot::loadEffectInner(const EffectManifestPointer pManifest,
     }
 
     // ControlObjects are 1-indexed
+    setCachedEffectName(m_group, pManifest->displayName());
     m_pControlLoadedEffect->setAndConfirm(m_pVisibleEffects->indexOf(pManifest) + 1);
 
     emit effectChanged();
@@ -372,6 +395,7 @@ void EffectSlot::unloadEffect() {
         return;
     }
 
+    setCachedEffectName(m_group, QString());
     m_pControlLoaded->forceSet(0.0);
     m_pControlLoadedEffect->setAndConfirm(0.0);
     for (const auto& pControlNumParameters : std::as_const(m_pControlNumParameters)) {
diff --git a/src/controllers/scripting/legacy/controllerscriptinterfacelegacy.h b/src/controllers/scripting/legacy/controllerscriptinterfacelegacy.h
--- a/src/controllers/scripting/legacy/controllerscriptinterfacelegacy.h
+++ b/src/controllers/scripting/legacy/controllerscriptinterfacelegacy.h
@@ -25,6 +25,7 @@ class ControllerScriptInterfaceLegacy : public QObject {
     Q_INVOKABLE QJSValue getSetting(const QString& name);
     Q_INVOKABLE double getValue(const QString& group, const QString& name);
     Q_INVOKABLE QString getTrackLocation(const QString& group);
+    Q_INVOKABLE QString getEffectName(const QString& group);
     Q_INVOKABLE void setValue(const QString& group, const QString& name, double newValue);
     Q_INVOKABLE double getParameter(const QString& group, const QString& name);
     Q_INVOKABLE void setParameter(const QString& group, const QString& name, double newValue);
diff --git a/src/controllers/scripting/legacy/controllerscriptinterfacelegacy.cpp b/src/controllers/scripting/legacy/controllerscriptinterfacelegacy.cpp
--- a/src/controllers/scripting/legacy/controllerscriptinterfacelegacy.cpp
+++ b/src/controllers/scripting/legacy/controllerscriptinterfacelegacy.cpp
@@ -4,6 +4,7 @@
 #include "control/controlobjectscript.h"
 #include "controllers/scripting/legacy/controllerscriptenginelegacy.h"
 #include "controllers/scripting/legacy/scriptconnectionjsproxy.h"
+#include "effects/effectslot.h"
 #include "mixer/playerinfo.h"
 #include "mixer/playermanager.h"
 #include "moc_controllerscriptinterfacelegacy.cpp"
@@ -152,6 +153,10 @@ QString ControllerScriptInterfaceLegacy::getTrackLocation(const QString& group) {
     return pTrack->getLocation();
 }
 
+QString ControllerScriptInterfaceLegacy::getEffectName(const QString& group) {
+    return EffectSlot::displayNameForGroup(group);
+}
+
 void ControllerScriptInterfaceLegacy::setValue(
         const QString& group, const QString& name, double newValue) {
     if (util_isnan(newValue)) {
